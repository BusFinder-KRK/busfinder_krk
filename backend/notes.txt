NOTES:

GENERAL============

=====================

STOPFINDER.CPP===============

=====================

ROUTING.CPP===============
- maybe later switch the line ID to be and integer since ints are faster: for exaple instead of doing "walk" do a -1 or sth; you need to make sure that line id for trams is just int
-should i delete the line_if from transport node?

- TO DO WITH WALKING (when i will want to maximize for speed):
        - in the walking_time.csv - the bigger number is always first so that there is only one hash look up
=====================

ROUTING.H===============
- add a constructor that changes the paramters
=====================